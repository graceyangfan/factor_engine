[package]
name = "factor_engine"
version = "0.1.0"
edition = "2021"
description = "Low-latency factor engine with offline/online parity validation"
license = "MIT"

[dependencies]
thiserror = "1"

[dev-dependencies]
approx = "0.5"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
